name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 9 */2 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Install PostgreSQL client
        run: sudo apt-get install -y postgresql-client

      - name: Ping Supabase via connection pooler
        run: |
          PGPASSWORD="Patrimonio753951!" psql \
            "postgresql://postgres.xsjeyqnpppnuojfeeglp:Patrimonio753951!@aws-0-us-east-1.pooler.supabase.com:6543/postgres" \
            -c "UPDATE patrimonio SET updated_at = NOW() WHERE id = 'main'; SELECT updated_at FROM patrimonio WHERE id = 'main';"
          echo "Ping OK"
